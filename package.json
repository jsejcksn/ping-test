{
  "name": "ping-test",
  "version": "0.9.0",
  "description": "Check if a an address is reachable and trigger webhook on state change",
  "main": "ping-test.js",
  "dependencies": {
    "post-json": "^0.3.1",
    "ping": "^0.1.10"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jesse Jackson"
}
