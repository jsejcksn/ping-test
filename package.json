{
  "name": "ping-test",
  "version": "0.9.1",
  "description": "Check if an address is reachable and trigger webhook on state change",
  "main": "index.js",
  "dependencies": {
    "humanize-duration": "^3.7.1",
    "moment": "^2.13.0",
    "ping": "^0.1.10",
    "post-json": "^0.3.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jesse Jackson"
}
